g-wan-static:
      restart: always
      image: iosifnicolae2/gwan-static
      ports:
        - ${PORT}:80
      environment:
        CLONE_GIT: ${git-repo}
        PORT: ${port}
        CLONE_PUBLIC_HTML: ${public-html}
